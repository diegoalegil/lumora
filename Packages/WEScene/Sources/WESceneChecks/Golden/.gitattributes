# Golden reference frames are raw RGBA byte blobs — never apply line-ending or text normalization, which
# would corrupt a frame that happens to contain a 0x0A/0x0D byte and break the byte-exact comparison.
*.rgba binary
